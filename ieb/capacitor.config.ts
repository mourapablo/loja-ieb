import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mourapablo.lojacordeiro',
  appName: 'Loja IEB no Cordeiro',
  webDir: 'build'
};

export default config;
